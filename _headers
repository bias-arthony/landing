# Cloudflare Pages: serve the profile file as real JSON.
# (GitHub Pages ignores this file; curl still returns the JSON body.)
/api/profile
  Content-Type: application/json; charset=utf-8
  Access-Control-Allow-Origin: *
